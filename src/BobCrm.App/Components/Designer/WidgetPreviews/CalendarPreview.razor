@using AntDesign
@using BobCrm.App.Models.Widgets

@* Calendar preview component (DatePicker visual hint) *@
<DatePicker Value="@(default(DateTime?))"
            ValueChanged="(DateTime? _) => { }"
            Picker="DatePickerType.Date"
            Disabled="true"
            Style="width:100%;" />

@code {
    [Parameter] public CalendarWidget? Widget { get; set; }
    [Parameter] public BobCrm.App.Models.FormRuntimeContext? Context { get; set; }
}
