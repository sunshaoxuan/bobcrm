# UI-04 实体定义画面改版说明（归档指引）

本文件原为“边开发边记录”的流水式文档。为提升可读性与可追溯性，设计内容已重构并迁移至以下文档：

| 主题 | 新文档 |
| --- | --- |
| 实体定义编辑器（主/子实体、接口、多语 UI） | `ARCH-20-实体定义管理设计.md` |
| 组织树、角色/权限、默认功能/菜单种子 | `ARCH-21-组织与权限体系设计.md` |

## 使用方式
1. 在阅读前先查阅 `process/PROC-00-文档索引.md` 获取最新目录；
2. 如果需要历史对比，可在 Git 历史中查看本文件被删除前的版本；
3. 若发现仍有内容未迁移，请将需求或讨论移至 `ARCH-20/21` 对应章节。

> **说明**：自本次重构起，UI/UE 与权限设计的权威描述以 `ARCH-20`、`ARCH-21` 为准；`UI-04` 仅作为“旧文档已迁移”的占位说明，不再追加内容。
